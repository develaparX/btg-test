# TEST LARAVEL - GOLANG

- 'docker compose up -d' in root for database
- setting .env on client and server like .env.example

then run :

cd server-golang && go run cmd/main.go
cd client-laravel && php artisan serve
